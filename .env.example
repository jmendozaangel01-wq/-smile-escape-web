# Anthropic API key — used ONLY server-side by the /api/maia serverless function.
# Do NOT prefix with VITE_ (that would inline it into the browser bundle and
# expose the key to every visitor). Set the real value in Vercel's dashboard,
# and for local dev copy this file to `.env` and fill it in.
ANTHROPIC_API_KEY=
